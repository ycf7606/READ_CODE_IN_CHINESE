# Stage 27 Summary

## Milestone

- Stage: 27
- Name: Rollback to stable Stage 22 baseline
- Date: 2026-04-15

## Stable Baseline

- Last known usable version: `5dedc4f`
- Milestone name: `Stage 22: recover panel and scale wordbook`
- Why this baseline:
  - it is the last version before the source-editor/panel-state hotfix chain started in Stage 23
  - the user-reported breakages around watched selection, wordbook visibility, and panel behavior all happened after this point
  - the rollback baseline compiles and passes tests cleanly

## Features Added After The Stable Baseline

- Stage 23:
  - source editor isolation from output/log editors
  - extra auto-explain deduplication and tracked-editor fallback logic
- Stage 24:
  - settings navigation polish
  - clearer preprocess visibility in the explain page
- Stage 25:
  - ready-time selection replay
  - tighter wordbook/progress resync logic
- Stage 26:
  - split between watch state and reveal state
  - extra wordbook cache diagnostics

## Rollback Action

- Reverted the `Stage 23-26` commit range and restored the repository to the Stage 22 code baseline.
- Verified the rollback baseline with:
  - `cmd /c npm.cmd run compile`
  - `cmd /c npm.cmd test`

## Replacement Route

- Do not keep layering more focus and panel-state branches directly inside `src/extension.ts`.
- Reintroduce the rolled-back capabilities using a different implementation path:
  - create a dedicated controller module for source-editor session state
  - define explicit transitions for:
    - tracked source editor
    - active selection signature
    - panel open state
    - panel visible state
    - preprocess job state
  - make the panel consume a pull-based snapshot keyed by `relativeFilePath + sourceHash` instead of relying on many scattered push updates
  - only re-add Stage 23-26 UX features after the controller path behaves correctly under real VS Code focus changes

## Notes

- This stage is intentionally a rollback milestone, not a feature milestone.
- The goal is to restart post-Stage-22 work from a known-good baseline instead of continuing to patch an increasingly fragile runtime state machine.
