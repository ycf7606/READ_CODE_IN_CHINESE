# Stage 28 Summary

## Milestone

- Stage: 28
- Name: Rollback to stable Stage 19 baseline
- Date: 2026-04-15

## Stable Baseline

- Last known usable version without `Current Selection`: `650fc81`
- Milestone name: `Stage 19: group wordbook by code scope`
- Why this baseline:
  - Stage 20 is the first milestone that introduced the top-of-page `Current Selection` focus card
  - the user explicitly confirmed that all versions containing `Current Selection` are unusable
  - the Stage 19 snapshot now compiles and passes tests cleanly after being pulled back into the current branch

## Features Added After This Baseline

- Stage 20:
  - qualified dotted-call symbol extraction such as `nn.Parameter` and `torch.empty`
  - top-of-page current-selection focus card
  - markdown rendering for explanation surfaces
  - glossary snapshot removal
- Stage 21:
  - hybrid regex + LSP glossary extraction
  - layered local/API wordbook UI
  - search and scope filtering
  - per-file wordbook expand-state persistence
- Stage 22:
  - panel script hardening
  - preprocess cache versioning
  - TypeScript AST-backed external extraction
  - deeper Python alias/decorator external extraction
  - near-selection filter and lazy tree hydration

## Rollback Action

- Pulled the Stage 19 code snapshot directly from git history instead of adding another revert-on-top-of-revert chain.
- Removed Stage 20-22 code additions from the active codebase.
- Verified the restored baseline with:
  - `cmd /c npm.cmd run compile`
  - `cmd /c npm.cmd test`

## Replacement Route

- Rebuild forward from Stage 19 instead of trying to preserve the Current Selection-era explanation surface.
- Keep these implementation rules for the rebuild:
  - no top-of-page Current Selection card
  - no panel/watch state branching growth inside `src/extension.ts`
  - move panel/watch/preprocess orchestration into a dedicated controller
  - reintroduce Stage 20-22 features one cluster at a time behind smaller helper modules
- Suggested rebuild order:
  - first: Stage 20 functional pieces only, excluding the Current Selection UI
  - second: Stage 21 extraction and wordbook-structure improvements
  - third: Stage 22 panel hardening and scalable extraction

## Notes

- This stage intentionally prioritizes a lower-feature but cleaner runtime baseline over preserving newer UI features.
- The repository history now contains both rollback milestones:
  - Stage 27: back to Stage 22
  - Stage 28: back to Stage 19 after confirming the Current Selection-era line itself is not acceptable
