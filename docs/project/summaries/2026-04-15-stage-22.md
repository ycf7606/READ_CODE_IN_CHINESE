# Stage 22 Summary

## Milestone

- Stage: 22
- Name: Panel recovery and scalable extraction
- Date: 2026-04-15

## Delivered

- Hardened the explanation-panel webview:
  - removed the higher-risk inline-markdown regex path that could break older/stricter webview runtimes
  - added panel-side script error reporting back into the extension logger
  - restored normal initialization so buttons, ready signaling, and watched-selection rendering can recover cleanly
- Added `builderVersion` invalidation to preprocess caches so stale wordbook cache files are ignored after structural upgrades
- Added TypeScript AST-backed external symbol extraction:
  - aliased imports
  - decorators
  - chained external calls
  - imported constructors
- Expanded Python external symbol extraction:
  - aliased imports
  - decorator usage
  - direct calls through imported aliases
- Improved large-wordbook behavior:
  - lazy tree hydration for collapsed branches
  - new `Near selection` filter

## Key Files

- `src/ui/explanationPanel.ts`
- `src/extension.ts`
- `src/knowledge/preprocessStore.ts`
- `src/knowledge/symbolPreprocessBuilder.ts`
- `src/analysis/glossary.ts`
- `src/analysis/typeScriptAstGlossary.ts`
- `src/test/index.test.ts`

## Verification

- `cmd /c npm.cmd test`

## Notes

- The most likely cause of the “all buttons dead” regression was a panel-side script failure during initialization. The webview now reports script/runtime failures back to the extension logger, so the next failure of this class should be directly diagnosable from logs instead of appearing as a silent UI freeze.
