# Stage 24 Summary

## Milestone

- Stage: 24
- Name: Panel navigation and preprocess visibility
- Date: 2026-04-15

## Delivered

- Made settings navigation from the explanation panel more robust:
  - settings reveals no longer preserve focus, so an explicit user click now brings the settings panel forward
  - added a command-URI path for the header `Settings` control inside the explanation webview
  - kept the existing postMessage path as a fallback
- Improved manual preprocessing visibility:
  - when preprocessing is triggered from settings, the explanation panel is brought forward
  - the explain page now shows an explicit preprocessing status sentence in addition to the detailed progress lines and bar
- Kept the watched-reading behavior unchanged for automatic explanation updates, so regular source-code reading still avoids unnecessary focus stealing

## Key Files

- `src/ui/explanationPanel.ts`
- `src/ui/settingsPanel.ts`
- `src/extension.ts`

## Verification

- `cmd /c npm.cmd run compile`
- `cmd /c npm.cmd test`

## Notes

- This stage targets the UI symptom layer on top of the Stage 23 runtime fix. If the `Settings` control still appears unresponsive after this change, the next thing to inspect is whether the target panel is being opened in a different editor group than expected on the user's VS Code layout.
