# Changelog

## 0.1.1 - 2026-04-21

- saved the cleaned runnable repository as the `v0.1.1` baseline
- removed stale project-history documents and the unused token knowledge builder
- kept the strict remote-preprocess verification and smoke script workflow

## 0.1.0 - 2026-04-21

- marked the first stable release baseline
- defaulted wordbook preprocessing to full file-local candidate coverage
- added strict remote-inference verification for preprocess chunks
- rejected incomplete remote preprocess responses instead of caching partial results
- added fallback-endpoint retry support for OpenAI-compatible calls
- simplified repository documentation and removed stale project-history files
- cleaned the build flow so each compile starts from a fresh `dist/`
